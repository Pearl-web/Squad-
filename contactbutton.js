// pop up code for when user completes contact form 
 function formdone(){
  document.getElementById("popup").innerHTML = "Received - we'll be in touch.";
  alert(document.getElementById("popup").innerHTML)
}
